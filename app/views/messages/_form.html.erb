<%= form_for(@message) do |f| %>
  <% if @message.errors.present? %>
    <div class="alert alert-danger">
      <%= alert %>
      <ul>
        <% @message.errors.full_messages.each do |full_message| %>
          <li><%= full_message %></li>
        <% end %> 
      </ul>
    </div>
  <% end %>


　<div class="form-group">
  <label for="mesasge_body" class="col-2 col-form-label">名前</label>
  <div class="col-10">
    <%= f.text_field :name , class: "form-control", placeholder:"Jane Doe"%>
  </div>
</div>
<div class="form-group">
    <label for="mesasge_body">年齢</label>
    <div class="col-10"><%= f.text_field :name , class: "form-control", placeholder:"20"%></div>
</div>  
<div class="form-group">
  <label for="mesasge_body" >内容</label>
    <div class="col-10"><%= f.text_area :body, class: "form-control", placeholder:"御内容をご入力ください" %></div>
</div>

  <%= f.submit class: "btn btn-sm btn-primary" %>
<% end %>
<br>